{
  "name": "campus-buzz",
  "version": "1.0.0",
  "private": true,
  "description": "Campus Buzz – monorepo root (backend + frontend)",
  "scripts": {
    "//": "No postinstall here on purpose. A host that installs from the repo root (Render, Railway) would otherwise pull the frontend's full toolchain into a backend deploy. Run `npm run install:all` by hand for local setup.",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start",
    "start:backend": "npm --prefix backend start",
    "build:frontend": "npm --prefix frontend run build"
  },
  "license": "ISC"
}
